<%= form_for(@magid) do |f| %>
  <% if @magid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@magid.errors.count, "error") %> prohibited this magid from being saved:</h2>

      <ul>
      <% @magid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
